### COMMON SSNL UTILITIES ###
import json
import os

##########


def safe_load_json(path: str) -> dict:
    """
    Ensures that a filepath exists before
    attempting to load
    """

    return dict
